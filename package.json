{
  "name": "mobiljuragan",
  "version": "0.0.0",
  "private": true,
  "description": "MobilJuragan MVP monorepo — Flutter customer app, Next.js admin dashboard, Express API, shared packages.",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
